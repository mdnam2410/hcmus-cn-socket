{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "renew package and run python file",
            "type": "shell",
            "command": "${workspaceFolder}/app/update.bat",// && py ${workspaceFolder}\\${relativeFile}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "isBackground": true
        }
    ]
}